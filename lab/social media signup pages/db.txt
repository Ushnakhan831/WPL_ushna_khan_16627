CREATE DATABASE social_signup;

USE social_signup;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    platform VARCHAR(50),
    firstname VARCHAR(50),
    lastname VARCHAR(50),
    email VARCHAR(100),
    password VARCHAR(100),
    gender VARCHAR(10),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
